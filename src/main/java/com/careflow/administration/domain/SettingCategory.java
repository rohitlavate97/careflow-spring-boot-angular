package com.careflow.administration.domain;

/**
 * Functional category for grouping system configuration settings (§38).
 */
public enum SettingCategory {
    GENERAL,
    FACILITY,
    CLINICAL,
    SCHEDULING,
    BILLING,
    SECURITY,
    MAINTENANCE
}
