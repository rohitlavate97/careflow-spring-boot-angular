package com.careflow.administration.domain;

/**
 * Supported data types for validating and parsing dynamic system settings (§38).
 */
public enum SettingDataType {
    STRING,
    NUMBER,
    BOOLEAN,
    JSON
}
